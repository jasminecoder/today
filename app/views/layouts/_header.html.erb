<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand">Home</a>
    <ul class="nav ">
         <% if user_signed_in? %>
            <li class="nav-item name">
                <span class="nav-link"><%= current_user.name %> </span>
            </li>
            <li class="nav-item">
              <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:'btn btn-outline-primary me-2' %>
            </li>
         <% else %>
            <li class="nav-item">
              <%= link_to 'Login', new_user_session_path, class:'btn btn-outline-primary me-2' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Sign-up', new_user_registration_path, class:'btn btn-primary' %>
            </li>
         <% end %>
      </ul>
  </div>
</nav>


